{
    "scripts": {
        "build": "html-minifier --collapse-whitespace --remove-comments --remove-optional-tags --remove-redundant-attributes --remove-script-type-attributes --remove-tag-whitespace --use-short-doctype --minify-css --minify-js --input-dir src --output-dir public",
        "build-new": "node src/build.js"
    },
    "dependencies": {
        "clean-css": "^4.2.3",
        "html-minifier": "^4.0.0",
        "uglifyjs": "^2.4.11"
    }
}
